import AdminLogin from "../AdminLogin";

export const metadata = { title: "Admin Login — Open Limits Design" };

export default function AdminLoginPage() {
  return (
    <main className="page">
      <section className="section">
        <AdminLogin />
      </section>
    </main>
  );
}
